---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-analyst
  namespace: regintel
  labels:
    app: document-analyst
    app.kubernetes.io/component: agent
spec:
  replicas: 2
  selector:
    matchLabels:
      app: document-analyst
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: document-analyst
    spec:
      serviceAccountName: regintel-agent
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      terminationGracePeriodSeconds: 60
      containers:
        - name: document-analyst
          image: ghcr.io/gauravbarge/regulatory-intelligence/document-analyst:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: regintel-config
            - secretRef:
                name: regintel-secrets
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            exec:
              command: ["python", "-c", "import document_analyst; print('ok')"]
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            exec:
              command: ["python", "-c", "import document_analyst; print('ok')"]
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: document-analyst-hpa
  namespace: regintel
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: document-analyst
  minReplicas: 1
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
